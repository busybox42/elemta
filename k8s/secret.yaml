apiVersion: v1
kind: Secret
metadata:
  name: elemta-secrets
type: Opaque
stringData:
  # LDAP authentication credentials
  auth.ldap.bind_dn: "cn=admin,dc=example,dc=com"
  auth.ldap.bind_password: "admin_password"  # Replace with actual password in production 